import React from 'react';
import Header from '../common/Header';

const LoginPage = () => {
  return (
    <React.Fragment>
      <Header />
      <div>login</div>
    </React.Fragment>
  );
};

export default LoginPage;
